# 🛡️ RBAC Service

![Python](https://img.shields.io/badge/Python-3.10%2B-blue?style=for-the-badge&logo=python)
![FastAPI](https://img.shields.io/badge/FastAPI-0.100%2B-009688?style=for-the-badge&logo=fastapi)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15%2B-336791?style=for-the-badge&logo=postgresql)
![Docker](https://img.shields.io/badge/Docker-Enabled-2496ED?style=for-the-badge&logo=docker)

Высокопроизводительный микросервис для управления пользователями и правами доступа (Role-Based Access Control). Проект реализует гибкую систему разграничения прав, административную панель и безопасную аутентификацию на базе JWT.

---

## 📑 Содержание
- [Функциональность](#-функциональность)
- [Технологический стек](#-технологический-стек)
- [Структура проекта](#-структура-проекта)
- [Установка и запуск](#-установка-и-запуск)
- [Конфигурация (.env)](#-конфигурация)
- [База данных и миграции](#-база-данных-и-миграции)
- [Тестирование](#-тестирование)
- [API Эндпоинты](#-api-эндпоинты)

---

## ⚙️  Функциональность

### 🔐 Аутентификация и Безопасность
*   **JWT Auth**: Выдача Access токенов (алгоритм HS256). _Refresh токены в будущем._
*   **Password Hashing**: Безопасное хранение паролей с использованием `bcrypt`.
*   **Dependency Injection**: Защита маршрутов через зависимости `get_current_user` и `check_permissions`.

### 👑 RBAC (Role-Based Access Control)
*   **Динамические роли**: Создание и удаление ролей "на лету" через БД.
*   **Гранулярный доступ**: Права назначаются не просто на роут, а на конкретные **Бизнес-Элементы** (сущности системы).
*   **Правила доступа (Access Rules)**: Гибкая привязка ролей к действиям (Create, Read, Update, Delete) над элементами.

### 🛠 Административная панель
*   Полный CRUD для управления пользователями.
*   Назначение и отзыв ролей.
*   Блокировка/разблокировка пользователей.

---

## 💻 Технологический стек

### Core & Web
*   **[FastAPI](https://fastapi.tiangolo.com/)**: Современный асинхронный веб-фреймворк.
*   **[Uvicorn](https://www.uvicorn.org/)**: ASGI-сервер для production-ready запуска.
*   **[Pydantic v2](https://docs.pydantic.dev/)**: Строгая валидация и сериализация данных.

### Database
*   **[PostgreSQL](https://www.postgresql.org/)**: Основное хранилище данных.
*   **[SQLAlchemy (Async)](https://docs.sqlalchemy.org/en/20/orm/extensions/asyncio.html)**: Асинхронная ORM для работы с БД.
*   **[Asyncpg](https://magicstack.github.io/asyncpg/)**: Быстрый драйвер для PostgreSQL.
*   **[Alembic](https://alembic.sqlalchemy.org/)**: Управление миграциями схемы базы данных.

### Utils & Testing
*   **[Loguru](https://github.com/Delgan/loguru)**: Красивое и структурированное логирование.
*   **[Python-Jose](https://github.com/mpdavis/python-jose)**: Работа с JWT токенами.
*   **[Pytest](https://docs.pytest.org/)**: Фреймворк для тестирования.
*   **[HTTPX](https://www.python-httpx.org/)**: Асинхронный HTTP-клиент для тестов.

---

## 📂 Структура проекта

```text
EffectiveMobile_test/
├── app/
│   ├── api/                 # Роутеры (эндпоинты) API
│   │   ├── admin.py         # Админ-панель
│   │   ├── auth.py          # Логин/Регистрация
│   │   ├── users.py         # Профиль пользователя
│   │   └── business_elements.py # Управление сущностями
│   ├── core/                # Ядро конфигурации
│   │   ├── config.py        # Pydantic Settings
│   │   └── security.py      # Хеширование и JWT
│   ├── database/            # Слой работы с БД
│   │   ├── session.py       # Engine и SessionMaker
│   │   └── init_db.py       # Начальный сидинг БД
│   ├── models/              # SQLAlchemy модели
│   │   ├── rbac.py          # Модели ролей и прав
│   │   └── users.py         # Модель пользователя
│   ├── schemas/             # Pydantic схемы (DTO)
│   └── main.py              # Точка входа приложения
├── alembic/                 # Файлы миграций
├── tests/                   # Pytest тесты
├── requirements.txt         # Зависимости
└── .env.example             # Пример конфигурации
```

---

## ⚡ Установка и запуск

### Вариант 1: Docker (Рекомендуется)
Проект полностью готов к запуску в контейнере.

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/Esmien/RBAC_FastAPI
   cd RBAC_FastAPI
   ```

2. **Создайте файл конфигурации:**
   ```bash
   cp .env.example .env
   # При необходимости отредактируйте .env
   ```

3. **Запустите через Docker Compose:**
   ```bash
   docker compose up --build -d
   ```
   *Приложение автоматически применит миграции и создаст начальные данные при старте.*

### Вариант 2: Локальный запуск
Требуется установленный PostgreSQL.

1. **Создайте виртуальное окружение:**
   ```bash
   python -m venv venv
   source venv/bin/activate  # Для Windows: venv\Scripts\activate
   ```

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Настройте `.env`:**
   Убедитесь, что `DATABASE_URL` указывает на вашу локальную базу данных.

4. **Примените миграции:**
   ```bash
   alembic upgrade head
   ```

5. **Запустите сервер:**
   ```bash
   python -m app.main
   ```

Сервер будет доступен по адресу: `http://127.0.0.1:8000`

---

## 🔧 Конфигурация

Все настройки хранятся в файле `.env`. Основные параметры:

| Переменная | Описание |
|------------|----------|
| `POSTGRES_USER` | Пользователь БД |
| `POSTGRES_PASSWORD` | Пароль БД |
| `POSTGRES_DB` | Имя базы данных |
| `DATABASE_URL` | Строка подключения (postgresql+asyncpg://...) |
| `SECRET_KEY` | Секретный ключ для подписи JWT |
| `ALGORITHM` | Алгоритм шифрования (по умолчанию HS256) |
| `ACCESS_TOKEN_EXPIRE_MINUTES` | Время жизни токена (мин) |

> 💡 **Совет:** Для генерации надежного `SECRET_KEY` используйте встроенный скрипт:
> ```bash
> python -m app.core.secret_key_gen
> ```

---

## 🗄️ База данных и миграции

Проект использует **Alembic** для версионирования схемы БД.

*   **Создать новую миграцию** (после изменения моделей):
    ```bash
    alembic revision --autogenerate -m "Описание изменений"
    ```
*   **Применить миграции:**
    ```bash
    alembic upgrade head
    ```
*   **Откатить последнюю миграцию:**
    ```bash
    alembic downgrade -1
    ```

При старте приложения (`lifespan` в `main.py`) автоматически запускается проверка миграций и инициализация БД (`init_db.py`).

---

## 🧪 Тестирование

Тесты написаны с использованием **Pytest** и **Pytest-Asyncio**. Покрывают основные сценарии авторизации и проверки прав доступа.

Запуск тестов:
```bash
pytest -v
```
Для корректной работы тестов может потребоваться отдельная тестовая база данных или настройка `conftest.py` на использование `SQLite` в памяти (в зависимости от текущей реализации `conftest.py`).

---

## 📡 API Эндпоинты

Интерактивная документация доступна после запуска:
*   📕 **Swagger UI**: `/docs`
*   📘 **ReDoc**: `/redoc`

### Основные группы маршрутов:
*   `/auth` — Регистрация и получение токена.
*   `/users` — Профиль пользователя, обновление данных.
*   `/admin` — Управление пользователями и ролями (Только для админов).
*   `/business-elements` — CRUD операции над защищенными ресурсами (Проверка RBAC).
